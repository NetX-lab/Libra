# Runtime dependencies for Megatron-Core + Qwen3-MoE Bridge on PyTorch 2.7/CUDA 12.6.
megatron-core==0.14.0
grouped_gemm==1.1.4
omegaconf>=2.3.0,<3.0.0
hydra-core>1.3,<=1.3.2
accelerate>=1.0.0,<2.0.0
rich>=13.0.0
six>=1.17.0
regex>=2024.11.6
tensorboard>=2.19.0
einops>=0.8.0,<1.0.0
packaging>=24.2

# megatron-bridge==0.2.0rc6 is intentionally installed with --no-deps by
# scripts/install_megatron_core.sh. Its broad "megatron-core[dev]" dependency
# otherwise installs CUDA 13 Transformer Engine packages on this CUDA 12 cluster.
