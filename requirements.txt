# Heterogeneous TP Configuration Testing Framework Dependencies

# Core ML/DL frameworks
# Qwen3-30B-A3B is a MoE model. vLLM 0.9.0 has a known broken
# MoE custom-op path for this model (`_moe_C.topk_softmax` missing).
# On the current HPC image, prebuilt vLLM wheels for 0.9.2 require
# GLIBC_2.29 for `vllm/_moe_C.abi3.so`; build vLLM locally so the MoE
# extension links against the cluster runtime instead of the wheel runtime.
--no-binary=vllm
vllm==0.9.2
torch==2.7.0
torchaudio==2.7.0
torchvision==0.22.0
xformers==0.0.30
transformers>=4.51.1,<5.0.0
pybase64==1.4.3
compressed-tensors==0.10.2
openai>=1.52.0,<=1.90.0
# Megatron-Core version matched by the Qwen3-MoE bridge integration.
# Megatron Bridge itself is installed with scripts/install_megatron_core.sh
# using --no-deps so it does not pull CUDA 13 Transformer Engine wheels.
megatron-core==0.14.0

cmake==3.26.4

# Dataset handling
datasets>=2.18.0,<3.0.0

# GPU monitoring
pynvml>=11.5.0
nvidia-ml-py>=12.555.0

# Data processing and analysis
pandas>=2.1.0,<3.0.0
numpy>=1.26.0,<2.0.0

# SciPy (pin version to avoid NumPy conflicts)
scipy>=1.11.0,<1.14.0

# Visualization
matplotlib>=3.8.0,<4.0.0
seaborn>=0.13.0,<1.0.0

# Configuration
pyyaml>=6.0.1

# Async HTTP client
aiohttp>=3.9.0,<4.0.0
httpx>=0.27.0,<1.0.0

# Progress bars
tqdm>=4.66.0

# Utilities
python-dateutil>=2.8.2
psutil>=5.9.8

# Logging
colorlog>=6.8.0

# Testing (optional)
pytest>=8.0.0
pytest-asyncio>=0.23.0
latex2sympy2
# word2number
wandb
sympy
